# UIManager

UIManager.cs handles the basic communication (changing printed data) with all of the UI elements. It does not control all the functionality of the UI - meaning buttons being clicked - because the action of the buttons is specialized to one of the other managers.